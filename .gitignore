.DS_Store
*.pyc
*.egg-info
*.swp
tags
lms/docs/current
lms/public/dist
__pycache__/
*.py[cod]
*$py.class
node_modules
package-lock.json
lms/public/frontend
lms/www/lms.html
lms/www/_lms.html
frappe-ui
frappe-semgrep-rules
# Material de los cursos descargado de Disco (PDFs, imagenes, portadas).
# El original vive en Disco y en el LMS; son decenas de MB que no aportan
# nada al repositorio.
docker/materiales/

# ZIPs de cursos exportados: se regeneran con docker/exportar_curso.py
docker/exportados/
# Ficheros intermedios de los generadores
docker/_*.txt

# Lo que saca auditar_stripe.py: nombres, correos y lo que pago cada alumna.
# Datos personales de gente real; no tienen por que acabar en GitHub.
auditoria_accesos.csv
auditoria_resumen.txt
migracion.json
